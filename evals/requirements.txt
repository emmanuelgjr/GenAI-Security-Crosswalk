# GenAI Security Crosswalk — eval tooling, version-pinned for reproducibility.
# Tool APIs churn fast; these pins are the versions the profiles/scripts were
# written and validated against. Bump deliberately and re-verify.
#
#   pip install -r evals/requirements.txt
#
# promptfoo is a Node tool, not pip — run via:  npx promptfoo@0.121.15
# Some models are gated (Meta Prompt Guard); run `huggingface-cli login`.

# Offensive / model-behavior
garak==0.15.1
pyrit==0.14.0

# Agentic (Inspect AI + upstream benchmarks: AgentDojo, AgentHarm)
inspect-ai==0.3.237
inspect_evals==0.13.2

# Supply chain — model artifact scanning
modelscan==0.8.8
picklescan==1.0.4

# Guardrail evaluation
nemoguardrails==0.22.0
transformers>=4.43
torch>=2.2

# Privacy / data-security (DSGAI)
presidio-analyzer==2.2.362
openai>=1.40
