# Classifier-proposed mappings

This directory contains auto-generated mapping proposals from the
classifier pipeline. Files are created by the GitHub Action when a
framework submission is processed.
